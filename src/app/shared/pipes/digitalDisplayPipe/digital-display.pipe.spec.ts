import { DigitalDisplayPipe } from './digital-display.pipe';

describe('DigitalDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new DigitalDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
